# Copy this to `.env` (gitignored) and fill in your key.
# Only DEEPGRAM_API_KEY is required to start a call.
# CRM/Calendar/Gmail access for the `/prompty-setup` skill is configured
# through your Claude Code MCPs, not here.

DEEPGRAM_API_KEY=
